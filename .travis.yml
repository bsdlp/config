language: go
sudo: required
go:
  - tip
before_install:
  - go get github.com/axw/gocov/gocov
  - go get github.com/mattn/goveralls
  - if ! go get code.google.com/p/go.tools/cmd/cover; then go get golang.org/x/tools/cmd/cover; fi
install:
  - go get gopkg.in/yaml.v2
before_script:
  - sudo mkdir -p /etc/testorg/testsystem/ && sudo chown -R $USER /etc/testorg/testsystem/
  - mkdir -p /home/travis/.config/testorg/testsystem && sudo chown -R $USER /home/travis/.config/testorg/testsystem
script:
  - $HOME/gopath/bin/goveralls -service=travis-ci
